# Changelog

Splendid Bookmarks for AI Agents の変更履歴です。書式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) に、
バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従います。

## [0.7.0] - 2026-09-22

### 追加

- `preparePlan` APIを追加。snapshot、scope、移動元ID・移動先ID・理由から、拡張機能がタイトル・URL・パス・操作IDを補完し、既存の計画読込とDry Runを1呼出しで実行
- 古いsnapshot、変更・消失したscope、範囲外、不明ID、不適切な移動先、同期境界不明、重複、干渉、no-opを提案全体として拒否する契約と回帰テストを追加

### 変更

- 表示名を「Splendid Bookmarks for AI Agents」に統一。管理画面、popup、toolbar、日英locale、README、プライバシーポリシー、ストア原稿・画像を更新
- 接続指示を既存Playwright CLI / MCP優先、必要時のみCDP接続へ整理。対象・セッション・scope照合後は画面操作の反復ではなく拡張APIを使う日英手順へ短縮
- 同じ合成提案で準備呼出しを2回から1回へ削減。JSON入力359→156 bytes、応答合計1010→768 bytesを実測。実トークン削減率は未測定

### 修正

- 計画読込中にsnapshotまたはscopeが変わった場合、拒否した提案の計画を残さず、別のUI読込で置き換わった計画は保持するよう修正
- schema不正または読込失敗の計画で`planDigest`だけが残らないよう、digestを受理済み計画にのみ設定

## [0.6.0] - 2026-09-22

### 変更

- API取得とコンテキストJSONの同期境界判定を統一。恒久ルートと同期属性が食い違う項目は境界不明とし、null同士を同一保存先と扱わない指示を追加
- ツリー取得・検索はページを確定してから返却行だけを生成し、全ツリーの不要な行変換を削減。ページ外の親情報・cursor・操作ロックを保持する回帰テストを追加

- 接続型指示文を6段階へ整理し、段階別の短い報告と候補0件時の終了条件を追加。JSON例をブックマーク移動・移動先ID必須の本文に合わせ、添付型と公開スキーマの互換性は維持

- 接続型指示文の冒頭に、終了・再起動の事前承認、ブラウザー前面化と選択タブ変更の禁止、未保存作業の保護、例外操作前の確認を日英で追加

- 接続型指示文にブラウザー種別、ツリー取得時刻・ダイジェスト、生成時刻・表示言語を自動挿入。利用者入力と未確認のプロファイル/CDP情報を分離し、既存接続の確認・再利用と不足時の案内を具体化

- 再取得後の画面構築に失敗した場合もAPIが成功を返さないよう修正。読込中にUIで別計画へ置き換えられたAPI要求は、別計画の結果を返さず拒否
- 接続型指示に実行可能な呼出定義・応答階層・適用と検証の成功条件を追加。実行バッチ上限をAPIで公開し、計画入力上限との混同を防止
- 実機検証のダウンロード監視と対話入力を、途中失敗・取消・タイムアウトでも閉じるよう改善

- 操作ナビゲーション、上部の読込・エラー状態、入力エラーの関連付けを追加。固定説明を折りたたみ、日英・明暗・320px幅とキーボード移動を実機検証
- 対象フォルダー消失時の全体への自動切り替えと、連続コピーによる古い通知・フォーカス変更を防止。対象再選択で回復可能にした
- バックアップのショートカットで計画とDry Runを保持。ダウンロード検証も画面表示ではなく実ファイルの作成と読み戻しを待つよう修正

- マネージャー最上部へエージェント指示のコピーボタンを追加。対象・整理目的・任意の接続先を近くに配置し、プレビューとJSON受け渡しを折りたたみに変更
- 接続型プロンプトを提案・相談・Dry Run・明示承認の流れに変更。JSON添付を不要とし、対象セッションと必要機能を照合
- 取得APIに境界と保護情報、`refreshTree`、構造化Dry Run/検証結果、適用時の任意計画ダイジェスト照合を追加。既存のバックアップ必須・操作ロックは維持
- 添付型の「移動のみ」と改名例の矛盾を解消し、対象選択だけでフォルダー空化を要求しないよう修正

## [0.5.0] - 2026-09-07

### 変更

- UIからのバッチ実行中もツリー読取APIを画面と同じ条件で拒否。`getSession` と取得CLIに実行モードを表示し、実行中の収集を完了扱いにしない回帰ガードを追加
- 日英のストア原稿・manifest概要・README・設計書を、コーディングエージェントからのブックマーク操作を主目的とする説明へ統一。接続条件と外部AIでのデータ処理も明記
- JSON連携セクションの名称と手順を整理し、公開版と開発版の読込動作に合わせてプライバシー説明を更新
- マネージャーを開くと未読込のブックマークツリーを自動取得。自動保存・自動適用・フォーカス移動は行わず、読込失敗時は手動で再試行できる
- Agent API の未読込エラーと日英の案内文を更新し、初期化待ちと API の不存在を区別
- `getTree` / `search` に世代固定のページ送りを追加。1ページ500件の上限を維持したまま全件取得でき、再読込後の古いcursorや別の検索条件への流用は拒否する
- `getSession` で読込状態とタブ固有IDを返し、読み取り専用CLIで接続先・利用者指定ラベルを明示して全件取得できるようにした
- 取得CLIは件数・世代・ID一意性を検証してから保存し、既存ファイルの上書きを拒否する。CDP接続処理も共通化し、終了時に待機中の処理とタイマーを解放する
- README の CWS 公開状態を更新し、公開版 0.4.0 と未リリース変更の違いを明記

## [0.4.0] - 2026-08-15

初回公開リリース。unpacked 読み込みでの配布です。

### 追加

- ライブツリーの取得と統計表示（URL 数、フォルダ数、ブラウザー判定、恒久ルート一覧、`syncing` 境界シグナル）
- ツリースナップショットの JSON 書き出し（SHA-256 付き）と、そのファイルだけを使った復元
- 重複検出（完全一致 / 正規化 URL）と、グループごとに残す 1 件を選んでの隔離移動
- 場所と絞り込みによる手動での移動選択（AI エージェント不要）
- Dry Run、適用、適用後の位置照合、逆順ロールバック
- Trash への退避とバッチ単位の復元（この段階は移動であって削除ではない）
- Trash からの永続削除。検証済みバックアップと確認チェックが揃った場合のみ、Trash 内の項目を 1 件ずつ照合しながら削除する
- エージェント用コンテキスト JSON の書き出しと、貼り付け用プロンプト
- 計画ファイルの公開スキーマ [extension/schemas/bookmark-plan.schema.json](extension/schemas/bookmark-plan.schema.json)
- UI の日本語 / 英語切り替え

### セキュリティ

- 書き込み経路は `extension/src/adapters/bookmarks-api.js` の 4 つだけ。`move`、Trash 用の空フォルダー作成、
  タイトルのみの `update`、Trash 内の単一 id 削除。`removeTree` は全面禁止
- 権限は `bookmarks` と `storage` の 2 つのみ。background service worker、ホスト権限、リモートコード、
  ネットワーク送信のいずれも持たない
- 上記の境界は [tests/no-write-api.test.js](tests/no-write-api.test.js) が実コードに対して機械的に固定し、
  検査自体が回避サンプルを検出できることも併せて検証する
- 1 バッチの上限は 200 操作。計画ファイルとスナップショットは untrusted input として扱い、読み込み前にサイズ上限を検査する

### 既知の制限

- アカウント / ローカルの同期境界越えの拒否は、境界が 2 つ実在するプロファイルでのみ検証できる。
  2026-08-14 の実測では境界が 1 つしか観測できず、**未検証のまま**
- 永続削除と外部からのツリー変更の間には TOCTOU が残る。詳細は [README](README.md) を参照
